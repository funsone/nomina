<%= form_for(@listum) do |f| %>
  <% if @listum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@listum.errors.count, "error") %> prohibited this listum from being saved:</h2>

      <ul>
      <% @listum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
  <%= f.label :sede_id %><br />
  <%= f.select :sede_id, options_for_select(Sede.all.map{|s|[s.nombre, s.id]}) %>
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
