<div class="page-header">
  <div class="pull-right">
    <%= link_to "Nuevo Concepto personal", new_conceptopersonal_path, class: "btn btn-danger"  %>
  </div>
  <h1>Conceptos Personales</h1>
</div>
<div class="panel panel-success">
  <div class="panel-heading">
    Asignaciones
  </div>
  <div class="panel-body">
    <div class="table-responsive">
      <table class="table table-bordered table-condensed table-responsive table-bordered">
        <thead>
          <tr>
            <th>Nombre</th>
            <th colspan="3">Acciones</th>
          </tr>
        </thead>

        <tbody>
          <%
          asignaciones=@conceptospersonales.where(tipo_de_concepto: 0).order("created_at")
          asignaciones.each do |concepto| %>
          <tr>
            <td><%= concepto.nombre %></td>
            <td><%= link_to '', concepto,class: "fa fa-eye" %></td>
            <td><%= link_to '', edit_conceptopersonal_path(concepto),class: "fa fa-pencil" %></td>
            <td><%= link_to '', concepto,class: "fa fa-trash", method: :delete, data: { confirm: 'Esta seguro?' } %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
<div class="panel panel-danger">
  <div class="panel-heading">
    Deducciones
  </div>
  <div class="panel-body">
    <div class="table-responsive">
      <table class="table table-bordered table-condensed table-responsive table-bordered">
        <thead>
          <tr>
            <th>Nombre</th>
            <th colspan="3">Acciones</th>
          </tr>
        </thead>

        <tbody>
          <%
  deducciones=@conceptospersonales.where(tipo_de_concepto: 1).order("created_at")
            deducciones.each do |concepto| %>
          <tr>
            <td><%= concepto.nombre %></td>
            <td><%= link_to '', concepto,class: "fa fa-eye" %></td>
            <td><%= link_to '', edit_conceptopersonal_path(concepto),class: "fa fa-pencil" %></td>
            <td><%= link_to '', concepto,class: "fa fa-trash", method: :delete, data: { confirm: 'Esta seguro?' } %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>