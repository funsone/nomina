<%= form_for(@cargo) do |f| %>
  <% if @cargo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cargo.errors.count, "error") %> prohibited this cargo from being saved:</h2>

      <ul>
      <% @cargo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :departamento_id %><br>
    <%= f.text_field :departamento_id %>
  </div>

  <h3>Sueldo</h3>
  <%= f.fields_for :sueldos do |sueldo_builder| %>
    <div>
      <%= sueldo_builder.label :monto %>
      <%= sueldo_builder.text_field :monto %>
    </div>
    <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
