<table>
  <tr>
    <th>Key</th>
    <th></th>
  </tr>
  <% @settings.each do |key,value| %>
  <tr>
    <td><%= key %></td>
    <td><%= value %></td>
    <td><%= link_to 'edit', edit_setting_path(key) %></td>
  </tr>
  <% end %>
</table>
